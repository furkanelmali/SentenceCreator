using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnPoses : MonoBehaviour
{

    public bool isthereword;
    // Start is called before the first frame update
    void Start()
    {
        isthereword = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
